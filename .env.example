# Only needed if the site moves to a custom domain. On Vercel the production
# domain is detected automatically from VERCEL_PROJECT_PRODUCTION_URL, so this
# can be left unset. No trailing slash.
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
